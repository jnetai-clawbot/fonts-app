Fonts-App

Update for these extra fonts and make sure they can all be converted with unicode to work on other apps and clipboard

project location /home/jay/Documents/Scripts/AI/OpenCode/Android-Fonts-App/

Add search box above the list of fonts (in the same div) so I can search (filter) the list of available (not hidden) fonts by its name, Put a Search icon aswel as-well as a clear search icon to clear the live search / filtering.

Remove any duplicate emojis and symbols

Remove emojis from symbols section (emojis should only be in emojis and symbols should only be in symbols).

use less padding in buttons as a letter gets cut off and theres plenty of room in padding inside the button to prevent the last letter going onto another line (rather it be responsive and adjust button text size to make it fit (this is only an issue for the 1st button at the bottom called Symbols as the s is on its own line, the other buttons seem fine! keep all padding in the bottom buttons the same so it looks consistant).

